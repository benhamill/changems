.alpha.grid_8#center
  h2 #{@gem.name} #{@gem.current_version_number}

  h3 Changes
  ul.all_versions
    - @versions.each do |version|
      li id=version.link_string
        h4= version.number
        .changes= version.release_notes

.omega.grid_4#side
  h3 Versions
  ul.versions
    - @versions.each do |version|
      li= link_to version.number, "##{version.link_string}"
